- flash.each do |key, message|
  = message

h1 Listing products

br
= link_to 'Users', users_path, class: 'btn btn-indigo'
br
= link_to '新規登録', new_product_path, class: 'btn btn-indigo'
a.btn.btn-indigo
  | ぼったん

.flex.flex-wrap.-mx-4
  = render(ProductComponent.with_collection(@products))
